package com.alex.binaryoperation;

public class BinaryOperationTemplate {
    public void operation(){

        // Basic operation
        /* ^ is xor
        *  a = 0 ^ a = a ^ 0
        *  0 = a ^ a
        * */

        // Swap 2 numbers
        /*
         *  a = a ^ b
         *  b = a ^ b
         *  a = a ^ b
         * */

        // Remove the last 1
        // a = n & (n - 1)

        // Obtain the last 1
        // diff = (n & (n - 1)) ^ n
    }
}
